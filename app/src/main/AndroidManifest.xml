<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.intfocus.template"
          android:hardwareAccelerated="false"
          android:largeHeap="true">

    <!-- 扫描条形码或二维码 -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- 6.0 及以上系统需要在代码中动态注册 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- 获取用户位置信息 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 必选 -->
    <uses-permission android:name="android.permission.INTERNET"/> <!-- 网络通信 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/> <!-- 获取MAC地址 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!-- 获取网络状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/> <!-- 获取设备信息 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/> <!-- 6.0 及以上系统需要在代码中动态注册 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/> <!-- 读sdcard，storage等等 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> <!-- 读写sdcard，storage等等 -->
    <!-- 6.0 及以上系统需要在代码中动态注册 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/> <!-- 允许程序录制音频 -->
    <uses-permission android:name="android.permission.GET_TASKS"/> <!-- 得到当前运行的activity名字 -->
    <!-- 5.0 及以上系统不可使用 -->

    <!-- 可选 -->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <!-- 获取logcat日志 -->
    <application
        android:name=".SYPApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${APPLICATIONID}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>

        </provider>

        <service android:name=".service.OpenUDIDService">
            <intent-filter>
                <action android:name="org.openudid.GETUDID"/>
            </intent-filter>
        </service>

        <!-- 定位服务 -->
        <service android:name=".service.LocationService"/>
        <!-- 高德地图 -->
        <service android:name="com.amap.api.location.APSService"/>

        <service android:name=".service.CollectionUploadService"/>

        <activity
            android:name=".launcher.LauncherActivity"
            android:screenOrientation="portrait"
            android:theme="@style/LauncherTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 模板三 -->
        <activity
            android:name=".subject.three.MultiIndexActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 模板九 -->
        <activity
            android:name=".subject.nine.CollectionActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 模板七 -->
        <activity
            android:name=".subject.seven.MyConcernActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN"/>-->

                <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".subject.seven.concernlist.ConcernListActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 公告详情 -->
        <activity
            android:name=".dashboard.mine.activity.NoticeContentActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 模板五 -->
        <!-- 忘记密码 -->
        <activity
            android:name=".login.ForgetPasswordActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 登录 -->
        <activity
            android:name=".login.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 修改密码页 -->
        <activity
            android:name=".dashboard.mine.activity.AlterPasswordActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 文章收藏页 -->
        <activity
            android:name=".dashboard.mine.activity.FavoriteArticleActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 扫码页 -->
        <activity
            android:name=".scanner.BarCodeScannerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
        <!-- 个人信息 -->
        <activity
            android:name=".dashboard.mine.activity.SettingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme2"/>
        <!-- 问题反馈 -->
        <activity
            android:name=".dashboard.feedback.FeedbackActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity
            android:name=".dashboard.feedback.FeedbackInputActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <!-- 个人信息页信息展示 -->
        <activity
            android:name=".dashboard.mine.activity.SettingListActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 选项配置 -->
        <activity
            android:name=".dashboard.mine.activity.SettingPreferenceActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>

        <!-- 主页面 -->
        <activity
            android:name=".dashboard.DashboardActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/AppDashboardTheme"/>

        <!-- 模板二/主题页面 -->
        <activity
            android:name=".subject.two.WebPageActivity"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme"/>

        <!-- 原生模板二/主题页面 -->
        <activity
            android:name=".subject.one.NativeReportActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>

        <!-- 原生模板二/表格页面 -->
        <activity
            android:name=".subject.one.module.tables.SubTableActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 评论页 -->
        <!-- 扫码结果页 -->
        <activity
            android:name=".scanner.ScannerResultActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 门店选择页 -->
        <activity
            android:name=".scanner.StoreSelectorActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 锁屏密码设置 -->
        <activity
            android:name=".dashboard.mine.activity.InitPassCodeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <!-- 锁屏登录 -->
        <activity
            android:name=".launcher.ConfirmPassCodeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".general.wxapi.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>
        <activity android:name=".dashboard.mine.activity.ShowListMsgActivity"/>
        <activity android:name=".dashboard.mine.InstituteContentActivity"/>
        <activity
            android:name=".dashboard.mine.activity.ShowPushMessageActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <!-- 消息页 -->
        <activity
            android:name=".dashboard.mine.activity.PushMessageContentActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <!-- 引导页 -->
        <activity
            android:name=".launcher.GuideActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <!-- 高德地图API - Key -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${AMAP_KEY}"/>

        <!-- 蒲公英 必选 -->
        <meta-data
            android:name="PGYER_APPID"
            android:value="${PGYER_APPID}"/>

        <!-- 友盟 必选 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="${UMENG_APPKEY}"/>
        <meta-data
            android:name="UMENG_MESSAGE_SECRET"
            android:value="${UMENG_MESSAGE_SECRET}"/>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="Umeng"/>

        <activity
            android:name=".ui.DiaLogActivity"
            android:theme="@style/myTransparent"/>
    </application>

</manifest>
