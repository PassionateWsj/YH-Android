package com.intfocus.template.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.intfocus.template.model.response.attention.AttentionFilterItem;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ATTENTION_FILTER_ITEM".
*/
public class AttentionFilterItemDao extends AbstractDao<AttentionFilterItem, Long> {

    public static final String TABLENAME = "ATTENTION_FILTER_ITEM";

    /**
     * Properties of entity AttentionFilterItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Main_data_id = new Property(1, String.class, "main_data_id", false, "MAIN_DATA_ID");
        public final static Property Main_data_name = new Property(2, String.class, "main_data_name", false, "MAIN_DATA_NAME");
    }


    public AttentionFilterItemDao(DaoConfig config) {
        super(config);
    }
    
    public AttentionFilterItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ATTENTION_FILTER_ITEM\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"MAIN_DATA_ID\" TEXT," + // 1: main_data_id
                "\"MAIN_DATA_NAME\" TEXT);"); // 2: main_data_name
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ATTENTION_FILTER_ITEM\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AttentionFilterItem entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String main_data_id = entity.getMain_data_id();
        if (main_data_id != null) {
            stmt.bindString(2, main_data_id);
        }
 
        String main_data_name = entity.getMain_data_name();
        if (main_data_name != null) {
            stmt.bindString(3, main_data_name);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AttentionFilterItem entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String main_data_id = entity.getMain_data_id();
        if (main_data_id != null) {
            stmt.bindString(2, main_data_id);
        }
 
        String main_data_name = entity.getMain_data_name();
        if (main_data_name != null) {
            stmt.bindString(3, main_data_name);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AttentionFilterItem readEntity(Cursor cursor, int offset) {
        AttentionFilterItem entity = new AttentionFilterItem( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // main_data_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // main_data_name
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AttentionFilterItem entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMain_data_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMain_data_name(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AttentionFilterItem entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AttentionFilterItem entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AttentionFilterItem entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
