package com.intfocus.template.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.intfocus.template.model.response.attention.AttentionItem;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ATTENTION_ITEM".
*/
public class AttentionItemDao extends AbstractDao<AttentionItem, Long> {

    public static final String TABLENAME = "ATTENTION_ITEM";

    /**
     * Properties of entity AttentionItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Attention_item_id = new Property(1, String.class, "attention_item_id", false, "ATTENTION_ITEM_ID");
        public final static Property Attention_item_name = new Property(2, String.class, "attention_item_name", false, "ATTENTION_ITEM_NAME");
    }


    public AttentionItemDao(DaoConfig config) {
        super(config);
    }
    
    public AttentionItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ATTENTION_ITEM\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"ATTENTION_ITEM_ID\" TEXT," + // 1: attention_item_id
                "\"ATTENTION_ITEM_NAME\" TEXT);"); // 2: attention_item_name
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ATTENTION_ITEM\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AttentionItem entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String attention_item_id = entity.getAttention_item_id();
        if (attention_item_id != null) {
            stmt.bindString(2, attention_item_id);
        }
 
        String attention_item_name = entity.getAttention_item_name();
        if (attention_item_name != null) {
            stmt.bindString(3, attention_item_name);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AttentionItem entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String attention_item_id = entity.getAttention_item_id();
        if (attention_item_id != null) {
            stmt.bindString(2, attention_item_id);
        }
 
        String attention_item_name = entity.getAttention_item_name();
        if (attention_item_name != null) {
            stmt.bindString(3, attention_item_name);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AttentionItem readEntity(Cursor cursor, int offset) {
        AttentionItem entity = new AttentionItem( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // attention_item_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // attention_item_name
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AttentionItem entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAttention_item_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAttention_item_name(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AttentionItem entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AttentionItem entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AttentionItem entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
