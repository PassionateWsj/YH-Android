package com.intfocus.template.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.intfocus.template.subject.seven.bean.ConcernListBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONCERN_LIST_BEAN".
*/
public class ConcernListBeanDao extends AbstractDao<ConcernListBean, Long> {

    public static final String TABLENAME = "CONCERN_LIST_BEAN";

    /**
     * Properties of entity ConcernListBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Uuid = new Property(1, String.class, "uuid", false, "UUID");
        public final static Property Obj_id = new Property(2, String.class, "obj_id", false, "OBJ_ID");
        public final static Property Obj_name = new Property(3, String.class, "obj_name", false, "OBJ_NAME");
        public final static Property Obj_num = new Property(4, String.class, "obj_num", false, "OBJ_NUM");
        public final static Property Rep_code = new Property(5, String.class, "rep_code", false, "REP_CODE");
        public final static Property Type = new Property(6, int.class, "type", false, "TYPE");
    }


    public ConcernListBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ConcernListBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONCERN_LIST_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"UUID\" TEXT," + // 1: uuid
                "\"OBJ_ID\" TEXT," + // 2: obj_id
                "\"OBJ_NAME\" TEXT," + // 3: obj_name
                "\"OBJ_NUM\" TEXT," + // 4: obj_num
                "\"REP_CODE\" TEXT," + // 5: rep_code
                "\"TYPE\" INTEGER NOT NULL );"); // 6: type
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONCERN_LIST_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ConcernListBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(2, uuid);
        }
 
        String obj_id = entity.getObj_id();
        if (obj_id != null) {
            stmt.bindString(3, obj_id);
        }
 
        String obj_name = entity.getObj_name();
        if (obj_name != null) {
            stmt.bindString(4, obj_name);
        }
 
        String obj_num = entity.getObj_num();
        if (obj_num != null) {
            stmt.bindString(5, obj_num);
        }
 
        String rep_code = entity.getRep_code();
        if (rep_code != null) {
            stmt.bindString(6, rep_code);
        }
        stmt.bindLong(7, entity.getType());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ConcernListBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindString(2, uuid);
        }
 
        String obj_id = entity.getObj_id();
        if (obj_id != null) {
            stmt.bindString(3, obj_id);
        }
 
        String obj_name = entity.getObj_name();
        if (obj_name != null) {
            stmt.bindString(4, obj_name);
        }
 
        String obj_num = entity.getObj_num();
        if (obj_num != null) {
            stmt.bindString(5, obj_num);
        }
 
        String rep_code = entity.getRep_code();
        if (rep_code != null) {
            stmt.bindString(6, rep_code);
        }
        stmt.bindLong(7, entity.getType());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ConcernListBean readEntity(Cursor cursor, int offset) {
        ConcernListBean entity = new ConcernListBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // uuid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // obj_id
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // obj_name
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // obj_num
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // rep_code
            cursor.getInt(offset + 6) // type
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ConcernListBean entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUuid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setObj_id(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setObj_name(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setObj_num(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setRep_code(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setType(cursor.getInt(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ConcernListBean entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ConcernListBean entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ConcernListBean entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
